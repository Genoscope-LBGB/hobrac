[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
# E = pycodestyle errors
# F = pyflakes
# I = isort
# W = pycodestyle warnings

[tool.ruff.lint.isort]
known-first-party = ["hobrac"]

[tool.snakefmt]
line_length = 88
