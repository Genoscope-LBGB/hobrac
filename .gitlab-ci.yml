variables:
  VERBOSITY: 10
  
include:
  - project: rdbioseq/cicd
    file: cicd.yml
    inputs:
      module-name: "hobrac"
      cd-branch: "master"
      other-branches-as-named: true

stages: 
  - deploy-futur

deploy-futur:
    stage: deploy
    extends: .deploy-futur
